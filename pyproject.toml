[project]
name = "online-boutique"
version = "0.1.0"
description = "CloudLab Online Boutique benchmark profile"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "geni-lib>=0.9.9.4",
]

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
